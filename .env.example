# Локальная база разработки (настраивается при onboarding).
# ВАЖНО: Prisma требует явного пользователя в URL — в отличие от psql,
# она НЕ подставляет пользователя ОС автоматически.
DATABASE_URL="postgresql://<пользователь-ОС>@localhost:5432/finflow_dev"

# Read-only доступ к prod-данным для операционного режима (выдаёт разработчик).
# DATABASE_URL_PROD_RO="postgresql://finflow_ro:<пароль>@<сервер>:5432/finflow_prod"
